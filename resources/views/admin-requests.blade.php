@extends('hrms-layout')
@section('content')<div class="page"><div class="crumb"><strong>Dashboard Admin</strong>　/　<strong>Danh sách PYC</strong></div><h1>Danh sách Phiếu yêu cầu</h1><p class="subtitle">Xem và xử lý các yêu cầu do nhân viên gửi.</p><div class="card" style="padding:0;overflow:hidden"><div class="table-wrap"><table class="data-table"><thead><tr><th>Mã phiếu</th><th>Người gửi</th><th>Nội dung</th><th>Ngày gửi</th><th>Trạng thái</th><th></th></tr></thead><tbody>@forelse($requests as $record)<tr><td><strong>#PYC-{{ str_pad($record->id,4,'0',STR_PAD_LEFT) }}</strong></td><td>{{ $record->creator->name ?? '—' }}</td><td>{{ $record->payload['title'] ?? $record->type->value }}</td><td>{{ $record->created_at?->format('d/m/Y') }}</td><td>@if($record->status->value==='pending')<span class="badge orange">Đang xử lý</span>@elseif($record->status->value==='rejected')<span class="badge red">Từ chối</span>@else<span class="badge green">{{ $record->status->value }}</span>@endif</td><td><a class="button light" href="{{ route('admin.requests.show',$record) }}">Xử lý</a></td></tr>@empty<tr><td colspan="6" class="empty">Chưa có phiếu yêu cầu.</td></tr>@endforelse</tbody></table></div></div></div>@endsection
