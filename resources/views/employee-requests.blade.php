@extends('hrms-layout')
@section('content')<div class="page"><div class="employee-crumb"><span>HỆ THỐNG NHÂN SỰ</span><b>/</b><strong>Phiếu yêu cầu</strong></div><div style="display:flex;justify-content:space-between;align-items:end;margin-bottom:20px"><div><h1>Danh sách Phiếu yêu cầu (PYC)</h1><p class="subtitle">Theo dõi các yêu cầu hỗ trợ và thủ tục nhân sự của bạn.</p></div><a class="button" href="{{ route('employee.requests.create') }}">＋ Tạo phiếu yêu cầu</a></div>@if(session('status'))<div class="notice">✓ {{ session('status') }}</div>@endif<div class="card" style="margin-top:20px;padding:0;overflow:hidden"><div class="table-wrap"><table class="data-table"><thead><tr><th>Mã phiếu</th><th>Tiêu đề</th><th>Loại</th><th>Ngày gửi</th><th>Trạng thái</th><th></th></tr></thead><tbody>@forelse($requests as $record)<tr><td><strong>#PYC-{{ str_pad($record->id,4,'0',STR_PAD_LEFT) }}</strong></td><td>{{ $record->payload['title'] ?? $record->type->value }}</td><td>{{ ['hardware' => 'Phần cứng', 'software' => 'Phần mềm', 'account' => 'Account', 'other' => 'Khác', 'profile_change' => 'Thay đổi hồ sơ'][$record->type->value] ?? $record->type->value }}</td><td>{{ $record->created_at?->format('d/m/Y') }}</td><td>@if($record->status->value === 'pending')<span class="badge orange">Đang xử lý</span>@elseif($record->status->value === 'completed')<span class="badge green">Hoàn thành</span>@else<span class="badge red">Từ chối</span>@endif</td><td><a class="button light" href="{{ route('employee.requests.show',$record) }}">Xem chi tiết</a></td></tr>@empty<tr><td colspan="6" class="empty">Bạn chưa tạo phiếu yêu cầu nào.</td></tr>@endforelse</tbody></table></div></div></div>@endsection
